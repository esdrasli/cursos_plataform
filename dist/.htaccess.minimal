# VERSÃO MÍNIMA - Use esta se as outras não funcionarem
# Renomeie para .htaccess

RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]

