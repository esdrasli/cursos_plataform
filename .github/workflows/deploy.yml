name: Deploy Backend

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Sync files to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "backend/"
          target: "/tmp/backend-deploy/"
          strip_components: 1
          command_timeout: 300s
          script_after: "exit 0"  # Sempre suceder mesmo com erro do .bashrc
          debug: false
      
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script_stop: false  # Não parar em caso de erro
          script: |
            # Ignorar erros do .bashrc e usar shell não-interativo
            export BASH_ENV=""
            set +e  # Não parar em caso de erro
            
            echo "🚀 Iniciando deploy do backend..."
            
            # Tentar encontrar o diretório do backend
            BACKEND_DIR="/opt/apps/cursos_plataform/backend"
            if [ ! -d "$BACKEND_DIR" ]; then
              BACKEND_DIR="/opt/storylinker/cursos_plataform/backend"
            fi
            if [ ! -d "$BACKEND_DIR" ]; then
              BACKEND_DIR=$(find /opt /home /var/www -type d -name "backend" 2>/dev/null | grep -i cursos | head -1)
            fi
            
            if [ -z "$BACKEND_DIR" ] || [ ! -d "$BACKEND_DIR" ]; then
              echo "❌ Diretório do backend não encontrado!"
              exit 1
            fi
            
            echo "✅ Diretório encontrado: $BACKEND_DIR"
            cd "$BACKEND_DIR"
            
            # Sincronizar arquivos do /tmp para o diretório do backend
            echo "📦 Sincronizando arquivos..."
            if [ -d "/tmp/backend-deploy" ]; then
              # Fazer backup dos arquivos importantes
              if [ -f ".env" ]; then
                cp .env .env.backup.$(date +%Y%m%d_%H%M%S) || true
              fi
              
              # Copiar arquivos (preservando .env e outros arquivos importantes)
              echo "📋 Copiando arquivos de /tmp/backend-deploy para $BACKEND_DIR..."
              rsync -av --exclude='.env' --exclude='.env.*' --exclude='node_modules' --exclude='dist' --exclude='.git' /tmp/backend-deploy/ "$BACKEND_DIR/" || {
                echo "⚠️  Rsync falhou, tentando cp..."
                cp -r /tmp/backend-deploy/* "$BACKEND_DIR/" 2>/dev/null || true
                cp -r /tmp/backend-deploy/.* "$BACKEND_DIR/" 2>/dev/null || true
              }
              
              # Limpar arquivos temporários
              rm -rf /tmp/backend-deploy
              echo "✅ Arquivos sincronizados"
            else
              echo "❌ Diretório /tmp/backend-deploy não encontrado!"
              echo "⚠️  O SCP pode ter falhado. Verificando se podemos continuar..."
              # Não falhar aqui, apenas avisar
            fi
            
            # Verificar se a rota está presente
            echo "🔍 Verificando se a rota create-checkout-session existe..."
            if grep -q "create-checkout-session" routes/checkout.routes.js 2>/dev/null; then
              echo "✅ Rota encontrada em routes/checkout.routes.js"
            elif grep -q "create-checkout-session" src/routes/checkout.routes.ts 2>/dev/null; then
              echo "✅ Rota encontrada em src/routes/checkout.routes.ts"
            else
              echo "⚠️  Rota não encontrada nos arquivos!"
            fi
            
            # Instalar dependências
            echo "📦 Instalando dependências..."
            npm install --production=false
            
            # Fazer build se usar TypeScript
            if [ -f "tsconfig.json" ]; then
              echo "🔨 Fazendo build TypeScript..."
              npm run build
              
              # Verificar se a rota está no arquivo compilado
              if [ -f "dist/routes/checkout.routes.js" ]; then
                if grep -q "create-checkout-session" dist/routes/checkout.routes.js; then
                  echo "✅ Rota encontrada no arquivo compilado"
                else
                  echo "⚠️  Rota não encontrada no arquivo compilado!"
                fi
              fi
            fi
            
            # Encontrar o processo PM2
            echo "📊 Verificando processos PM2..."
            PM2_APP=$(pm2 list | grep -E "cursos-api|backend" | awk '{print $2}' | head -1)
            
            if [ -z "$PM2_APP" ]; then
              echo "⚠️  Processo PM2 não encontrado. Listando todos os processos:"
              pm2 list
              echo "❌ Não foi possível reiniciar o PM2 automaticamente"
              exit 1
            fi
            
            echo "✅ Processo PM2 encontrado: $PM2_APP"
            
            # Mostrar qual arquivo está rodando
            echo "📋 Informações do processo:"
            pm2 show "$PM2_APP" | grep -E "script|exec_mode|status" || true
            
            # Reiniciar PM2
            echo "🔄 Reiniciando PM2..."
            pm2 restart "$PM2_APP" --update-env
            
            # Aguardar um pouco para o processo iniciar
            sleep 3
            
            # Verificar status
            echo "📊 Status do PM2:"
            pm2 status
            
            # Mostrar últimas linhas do log
            echo "📋 Últimas linhas do log:"
            pm2 logs "$PM2_APP" --lines 10 --nostream || true
            
            echo "✅ Deploy concluído com sucesso!"
