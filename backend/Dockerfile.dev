# Backend Dockerfile para Desenvolvimento
FROM node:20-alpine

WORKDIR /app

# Copiar arquivos de dependências
COPY package*.json ./
COPY tsconfig.json ./

# Instalar dependências
RUN npm ci

# Copiar código fonte (será montado como volume em dev)
COPY src ./src

# Expor porta
EXPOSE 3001

# Comando para desenvolvimento com hot-reload
CMD ["npm", "run", "dev"]

