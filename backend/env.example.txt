# Porta do servidor
PORT=3001

# JWT Secret
JWT_SECRET=seu_jwt_secret_super_seguro_aqui

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=cursos_plataform

# Ambiente
NODE_ENV=development

# Gateway de Pagamento
# Opções: mercadopago, stripe, pagseguro
PAYMENT_GATEWAY=mercadopago
PAYMENT_API_KEY=sua_access_token_mercadopago_aqui
PAYMENT_API_SECRET=sua_secret_key_aqui
PAYMENT_WEBHOOK_URL=http://localhost:3001/api/checkout/webhook

# SFTP Configuration (para upload de vídeos para servidor remoto)
# OBRIGATÓRIO: Configure pelo menos username + password OU username + chave SSH
SFTP_HOST=195.35.16.131
SFTP_PORT=22
SFTP_USERNAME=seu_usuario

# Opção 1: Usar senha (mais simples)
SFTP_PASSWORD=sua_senha

# Opção 2: Usar chave SSH (mais seguro) - descomente uma das opções abaixo
# SFTP_KEY_PATH=/caminho/para/sua/chave_privada
# OU
# SFTP_PRIVATE_KEY=conteudo_da_chave_privada_aqui
